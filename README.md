# Neap
A simple TOTP authenticator.

# build
```bash
flutter build apk --split-debug-info --obfuscate  --split-per-abi #(--build-number 80)
```
# Todo
* sort
* more feature...

# Warning
Neap now supports backup functionality, but it is not compatible with third-party tools like SpeedBackup. **PLEASE back up your data manually**.